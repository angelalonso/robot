struct Bar {
    val: i32
}
pub struct Foo {
    val: Vec<Bar>
}
impl Foo {
    pub fn new(val: Vec<Bar>) -> Self {
        Self {
            val
        }
    }
    pub fn set_field(&mut self, new_val: Vec<Bar>) {
        self.val = new_val;
    }
    pub fn val(&self) -> Vec<Bar> {
        self.val
    }
}
foreign_class!(class Foo {
    self_type Foo;
    constructor Foo::new(_: Vec<i32>) -> Foo;
    fn Foo::set_field(&mut self, _: Vec<i32>);
    fn Foo::val(&self) -> Vec<i32>;
});
